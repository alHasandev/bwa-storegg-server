<table id="example2" class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>NO</th>
      <th>Nama Koin</th>
      <th>Jumlah Koin</th>
      <th>Harga</th>
      <th>Aksi</th>
    </tr>
  </thead>
  <tbody>
    <% for(let i = 0; i < nominals.length; i++) { %> 
      <tr>
        <td><%= i + 1 %></td>
        <td><%= nominals[i].coinName %></td>
        <td><%= nominals[i].coinQuantity %></td>
        <td><%= nominals[i].price %></td>
        <td>
          <form action="/nominal/delete/<%= nominals[i]._id %>?_method=DELETE" method="post" onsubmit="return confirm('Apakah anda yakin akan menghapus data ?')">
            <a href="/nominal/edit/<%= nominals[i]._id %>" class="btn btn-sm btn-success">
              <i class="fas fa-pencil-alt"></i>
            </a>
            <button class="btn btn-sm btn-danger">
              <i class="fas fa-trash-alt"></i>
            </button>
          </form>
        </td>
      </tr>
    <% } %> 
  </tbody>
  </tfoot>
</table>
